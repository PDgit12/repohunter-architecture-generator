# Docker Compose runtime settings
# Copy this file to .env before running compose if desired.

# Comma-separated architecture API keys (supports rotation)
ARCHITECTURE_API_KEYS=replace_me_key_v1,replace_me_key_v2

# Cloud expert API key (used if your /chat path proxies to cloud expert)
EXPERT_API_KEY=replace_me_expert_secret
